version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - uploaded_images:/app/static/uploads
      - processed_images:/app/static/processed
    restart: unless-stopped

volumes:
  uploaded_images:
  processed_images:
